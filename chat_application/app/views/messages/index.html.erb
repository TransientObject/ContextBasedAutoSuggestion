<h1>Chat</h1>

<ul id="chat">
  <%= render @messages %>
</ul>

<%= form_for Message.new, remote: true do |f| %>
  <%= f.text_area :content, :rows => 4, :cols => 50 %>
  <%= f.submit "Send" %>
<% end %>

<%= subscribe_to "/messages/new" %>